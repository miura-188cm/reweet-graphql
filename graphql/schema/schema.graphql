type Tag {
  id:ID!
  name:String!
  count:Int!
}

type Query {
  allOwnedTags(userId:ID!):[Tag!]! #配列の強制, 空配列許容
}

query AllOwnedTags($userId: ID!) {
  allOwnedTags(userId: $userId) { id name count }
}
